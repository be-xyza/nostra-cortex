# Verification Checklist — Initiative 123

## Portfolio Integrity
- [ ] `RESEARCH_INITIATIVES_STATUS.md` has no conflict markers.
- [ ] `119-nostra-commons` remains completed.
- [ ] `123-cortex-web-architecture` is indexed and active.

## Runtime and Host Parity
- [ ] Gateway `/api/system/build` and `/api/system/ready` return healthy payloads.
- [ ] Desktop and web display the same graph root hash for identical corpus.
- [ ] Recommended path parity confirmed for `stable-cortex-domain`.

## Governance
- [ ] Mutating pipeline action blocked without approval envelope.
- [ ] Mutating pipeline action succeeds with steward envelope.
