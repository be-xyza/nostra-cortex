# Evidence Promotion

This directory is the governed destination for immutable evidence promoted out of mutable runtime outputs such as `logs/*`.

Use:

```bash
bash /Users/xaoj/ICP/scripts/promote_evidence_artifact.sh --source <path> --initiative 132-eudaemon-alpha-initiative
```

Keep the original runtime command contract alongside promoted evidence so the artifact remains reproducible and source-linked.
