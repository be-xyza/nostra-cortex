# Nostra Workspace

Canonical platform-layer workspace for Nostra.

## Owns
- Platform authority contracts and schemas.
- Platform canister/backend and frontend surfaces.
- Platform libraries (`nostra-*`).

## Does Not Own
- Cortex execution shell/runtime crates (`cortex-*`).
- Execution apps under `cortex/apps/*`.
