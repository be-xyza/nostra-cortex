# libraries/

Top-level shared/legacy external libraries consumed by first-party workspaces.

## Canonical Rule
- Cortex-owned libraries belong in `cortex/libraries`.
- Nostra-owned libraries belong in `nostra/libraries`.
