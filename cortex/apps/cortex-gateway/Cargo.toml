[package]
name = "cortex-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-eudaemon = { path = "../cortex-eudaemon" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
